# Backend API base URL
# For dev: keep as /api and use the Vite proxy (configured in vite.config.js).
# For production: set the full backend URL (https://indexmoney-api.onrender.com/api or your own).
VITE_API_BASE_URL=/api
